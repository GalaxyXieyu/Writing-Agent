## 写作助手前端（AiEditor）环境变量示例
# 说明：复制本文件为 .env（同目录），并填入你的实际值。
# 使用方式（在本目录 deploy/ 下）：
#   docker compose --env-file ./.env up -d --build

# 选择 Provider 类型（贴合 AiEditor 官方文档）：
# 可选：openai | gitee | spark | wenxin
VITE_AI_PROVIDER_TYPE=openai

# 通用：模型名称（多个用英文逗号分隔，默认取第一个）
# 例如：gpt-4o-mini、glm-4-flashx、moonshot-v1-8k、qwen2.5-72b-instruct 等
VITE_AI_MODEL=

# ========== openai/gitee 通用 ==========
# OpenAI 兼容网关根地址；
# - openai: 作为 endpoint 使用（AiEditor 自动补 /v1/chat/completions）
# - gitee: 若为根地址，将自动追加 /v1/chat/completions；也可直接填完整 /chat/completions
VITE_AI_BASE_URL=

# API Key（仅直连模式需要；生产建议走后端代理，避免暴露在前端）
VITE_AI_API_KEY=

# ========== spark（星火）专用，可选 ==========
# App 信息（按需填写，若使用 spark）
VITE_SPARK_APP_ID=
VITE_SPARK_API_KEY=
VITE_SPARK_API_SECRET=
# 版本：v2.1 | v3.1 | v3.5 | v4.0（默认 v3.5）
VITE_SPARK_VERSION=v3.5
# 协议：ws 或 wss（留空使用默认）
VITE_SPARK_PROTOCOL=

# ========== wenxin（文心）专用，可选 ==========
VITE_WENXIN_ACCESS_TOKEN=
# 其他可选参数
VITE_WENXIN_PROTOCOL=
VITE_WENXIN_VERSION=
